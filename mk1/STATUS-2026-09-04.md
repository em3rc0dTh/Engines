# MK1 — Current Architecture Snapshot

Date: 2026-09-04
Active branch: `build/mk1-b2-customer-soft-duplicate-resolution`

## Core architecture rule

**Engines comes first. CTA is independent and replaceable.**

```text
CLI ───────────┐
HTTP/API ──────┤
WebChat ───────┤
Telegram ──────┤── CTA boundary ── ENGINES
WhatsApp ──────┘
```

Telegram and WhatsApp add transport/connection behavior only. They do not add a Telegram/WhatsApp Customer model, Workflow, Services policy or Scheduler.

## Current platform truth

```text
STEP 1 — CTA / CHANNELS + CUSTOMER
CLI / HTTP-Postman historical milestones              ✅
WebChat C1A                                             ✅ CERTIFIED + HUMAN VERIFIED
WebChat C1B durable semantics                           ✅ CERTIFIED + HUMAN RESTART VERIFIED
Customer Registration Policy V2                        ✅ BUILT / PROVEN IN LOCAL E2E
Customer Registration Channel Core                      ✅ BUILT / PROVEN IN LOCAL E2E
B2 Customer soft-duplicate resolution                  ✅ CERTIFIED
Telegram registration local path                        ✅ AUTOMATED PASS + HUMAN VERIFIED
WhatsApp sender-phone behavior                          ✅ AUTOMATED PASS + HUMAN VERIFIED
WhatsApp B2 duplicate completion                        ✅ AUTOMATED PASS / 🧪 HUMAN RETEST PENDING
Combined C2/C4 local human E2E                          ⏳ NOT YET CLOSED
Real Telegram Bot API                                   ⚪ NOT CERTIFIED
Real Meta Cloud API / Kapso                             ⚪ NOT CERTIFIED

STEP 3 — SERVICES
S0–S7                                                  ✅ CERTIFIED
S8 final G1 clean closure                              ⏭ PENDING

STEP 4 — SCHEDULER
Services/Scheduler boundary                            ✅ DESIGNED
Scheduler runtime                                      ⚪ NOT CERTIFIED

STEP 5 — INTEGRATION
Boundary                                               ✅ DESIGNED
Provider runtime                                       ⚪ NOT CERTIFIED

AGENT / MCP / LLM ROUTING                              ❌ INTENTIONALLY ABSENT
```

## Current messaging Workflow

The first Telegram/WhatsApp business slice remains intentionally limited to:

```text
RegisterNewCustomer
```

Current V2 minimum:

```text
customer.name
customer.contact.phone
customer.contact.email
```

That requirement lives in Customer/Temporal policy, never in provider code.

## Local composition proven

```text
provider-shaped local conversation
        ↓
TelegramAdapter
        OR
MetaCloudApiTransport HMAC verification → WhatsAppAdapter
        ↓
CanonicalChannelEnvelope
        ↓
CustomerRegistrationChannelExecutionCore
        ↓
PostgreSQL durable channel ledger
        ↓
Temporal RegisterNewCustomer
        ↓
Customer persistence/audit
```

Original local-interactive automated authority:

```text
Source SHA   bfccd4a795400d2311201a880453b61b08d0b56a
Run          33896424897
Job          101100019937
Artifact     9945929946
SHA256       92b883ba035987274fbd7cc84f33b574118239eb19fa13990c4ec32a72e59c1e
Result       SUCCESS
```

## Human Telegram evidence

Eduardo executed two Telegram local runs.

The first completed normal registration through real Temporal as `CREATED`.

The second intentionally supplied alphabetic phone material. The channel returned:

```text
ERROR: CHANNEL_EVENT_INVALID
The same Temporal Workflow remains active; enter a new value.
```

The same Workflow remained in `WAITING_FOR_REQUIRED_DATA`, accepted a later simulated contact share plus valid email, and completed `CREATED`.

Therefore:

```text
C2 Telegram local interactive deterministic E2E ✅ HUMAN VERIFIED
```

Personal phone/email material is intentionally omitted from public repository evidence.

## Human WhatsApp discovery

The human WhatsApp run correctly started with:

```text
knownFields   = customer.contact.phone
missingFields = customer.name, customer.contact.email
```

The phone came from the locally HMAC-verified synthetic sender and was not requested again.

The entered email matched the Customer created during the earlier Telegram run. The canonical Customer policy correctly classified this as `SOFT_MATCH`, exposing:

```text
WAITING_FOR_DUPLICATE_DECISION
nextAction = RESOLVE_DUPLICATE
```

At that moment the pre-B2 simulator failed closed because no deterministic duplicate-resolution domain Update existed. That was a Customer Engine prerequisite gap, not a WhatsApp adapter failure.

## B2 Customer soft-duplicate resolution ✅ CERTIFIED

B2 now closes that gap with transport-independent canonical action:

```text
RESOLVE_CUSTOMER_DUPLICATE
  ├── USE_EXISTING
  └── CREATE_NEW
```

Runtime authority:

```text
Certified source  36afff68af3237bd6431fd643d7d969e5452a296
Run               33899907141
Job               101111281727
Result            SUCCESS
Artifact          9947248334
SHA256            2c02680d9e268957924303ab1113d71f0d872319b611a6ede3faca0a01ed678a
```

The clean B2 run proved:

```text
Telegram seed → Customer CREATED
WhatsApp same email → SOFT_MATCH
Temporal → WAITING_FOR_DUPLICATE_DECISION / RESOLVE_DUPLICATE
single candidate exposed
USE_EXISTING → ALREADY_EXISTS
same Customer ID as Telegram seed
no redundant WhatsApp phone prompt
```

The full domain probe also proved invalid-candidate rejection, explicit `CREATE_NEW`, and exact canonical duplicate-decision event replay with no second business effect.

Receipt: `Build/evidence/b2-customer-soft-duplicate-resolution-certification-2026-09-04.md`.

## Current human gate

Only one targeted human action remains for local C2/C4 closure:

```text
WhatsApp on B2 branch
  → use an email already created by Telegram
  → observe WAITING_FOR_DUPLICATE_DECISION / RESOLVE_DUPLICATE
  → choose Use existing
  → same Workflow completes ALREADY_EXISTS
  → MESSAGING_LOCAL_E2E_PASS
```

After that observation, and only then, the combined claim may become:

```text
C2/C4 LOCAL INTERACTIVE DETERMINISTIC E2E ✅ HUMAN VERIFIED
```

## Official-provider boundary

No real provider has been certified by these local gates.

WhatsApp remains official-route-only. Browser automation, WhatsApp Web session emulation, QR-session scraping and reverse-engineered clients remain rejected.

Meta Cloud API versus Kapso is still a provider implementation choice behind `WhatsAppTransportPort`.

Telegram real proof will use the official Telegram Bot API later.

## Current Step-3 truth

Services S7 remains certified on `build/mk1-s7-appointment-services-integration`. Appointment consumes canonical Services reads and freezes revision-aware Service/Offering selection state. Scheduler authority remains separate.

## Non-claims

This snapshot does not claim:

- real Telegram Bot API proof;
- a real Telegram token/account proof;
- real Meta Cloud API proof;
- real Kapso proof;
- public webhook reachability;
- combined C2/C4 human local verification yet;
- automatic contact merge into existing Customers;
- automatic multi-candidate duplicate adjudication;
- Appointment over messaging;
- advisor handoff over messaging;
- outbound campaigns/promotions;
- Scheduler runtime certification;
- Agent/MCP availability;
- production readiness.
