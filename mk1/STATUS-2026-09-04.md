# MK1 — Current Architecture Snapshot

Date: 2026-09-04
Active branch: `build/mk1-c2-telegram-bot-api-official`

## Core architecture rule

**Engines comes first. CTA is independent and replaceable.**

```text
CLI ───────────┐
HTTP/API ──────┤
WebChat ───────┤
Telegram ──────┤── CTA boundary ── ENGINES
WhatsApp ──────┘
```

Telegram and WhatsApp add transport/connection behavior only. They do not add a Telegram/WhatsApp Customer model, Workflow, Services policy or Scheduler.

## Current platform truth

```text
STEP 1 — CTA / CHANNELS + CUSTOMER
CLI / HTTP-Postman historical milestones              ✅
WebChat C1A                                             ✅ CERTIFIED + HUMAN VERIFIED
WebChat C1B durable semantics                           ✅ CERTIFIED + HUMAN RESTART VERIFIED
Customer Registration Policy V2                        ✅ BUILT / PROVEN IN LOCAL E2E
Customer Registration Channel Core                      ✅ BUILT / PROVEN IN LOCAL E2E
B2 Customer soft-duplicate resolution                  ✅ CERTIFIED
Telegram local registration                             ✅ AUTOMATED PASS + HUMAN VERIFIED
WhatsApp local registration                             ✅ AUTOMATED PASS + HUMAN VERIFIED
Combined C2/C4 local human E2E                          ✅ HUMAN VERIFIED
C2P Telegram official Bot API runner                    ✅ BUILT / DETERMINISTIC PASS
C2P real Telegram Bot API                               🧪 PHYSICAL TEST NOW
Real Meta Cloud API / Kapso                             ⚪ NOT CERTIFIED

STEP 3 — SERVICES
S0–S7                                                  ✅ CERTIFIED
S8 final G1 clean closure                              ⏭ PENDING

STEP 4 — SCHEDULER
Services/Scheduler boundary                            ✅ DESIGNED
Scheduler runtime                                      ⚪ NOT CERTIFIED

STEP 5 — INTEGRATION
Boundary                                               ✅ DESIGNED
Provider runtime                                       ⚪ NOT CERTIFIED

AGENT / MCP / LLM ROUTING                              ❌ INTENTIONALLY ABSENT
```

## Current messaging Workflow

The first Telegram/WhatsApp business slice remains intentionally limited to:

```text
RegisterNewCustomer
```

Current V2 minimum:

```text
customer.name
customer.contact.phone
customer.contact.email
```

That requirement lives in Customer/Temporal policy, never in provider code.

## Local C2/C4 closure

The provider-shaped local composition is already both automated and human verified:

```text
Telegram/WhatsApp presentation
→ adapter
→ CanonicalChannelEnvelope
→ CustomerRegistrationChannelExecutionCore
→ PostgreSQL durable channel ledger
→ real Temporal RegisterNewCustomer
→ Customer persistence
```

Human Telegram evidence included invalid-phone rejection with the same Workflow remaining active. Human WhatsApp evidence included verified sender-phone prefill with no redundant phone request. Both channels completed real Customer registration.

Human evidence: `Test/c2-c4-local-interactive-human-verification-2026-09-04.md`.

## B2 Customer soft-duplicate resolution ✅ CERTIFIED

Runtime authority:

```text
Certified source  36afff68af3237bd6431fd643d7d969e5452a296
Run               33899907141
Job               101111281727
Artifact          9947248334
SHA256            2c02680d9e268957924303ab1113d71f0d872319b611a6ede3faca0a01ed678a
```

B2 keeps duplicate truth inside Customer/Temporal and exposes canonical `RESOLVE_CUSTOMER_DUPLICATE` with `USE_EXISTING` / `CREATE_NEW`.

## C2P — official Telegram Bot API

C2P is the first gate that connects the channel stack to a real external messaging-provider API.

Architecture:

```text
real Telegram user
  ↓
Telegram Bot API official HTTPS
  ↓
getUpdates long polling
  ↓
TelegramAdapter
  ↓
CanonicalChannelEnvelope
  ↓
CustomerRegistrationChannelExecutionCore
  ↓
PostgreSQL durable channel ledger
  ↓
Temporal RegisterNewCustomer
  ↓
Customer persistence
```

The runtime contains an official Bot API HTTP client and a local long-poll runner. It uses only Bot API methods required for this physical slice: `getMe`, `getWebhookInfo`, `getUpdates`, `sendMessage` and `answerCallbackQuery`.

The runner fails closed if another webhook deployment is active, keeps `businessSlug` in trusted deployment configuration, never logs the bot token and rejects a shared contact whose Telegram `user_id` conflicts with the actual message sender.

### Deterministic C2P authority

```text
Source SHA   a0a9b241aab5ffb4fc029179052cd4a80d764620
Run          33906116526
Job          101131331563
Result       SUCCESS
Artifact     9949550064
SHA256       6e8606c4ab6082691e8f5699b2d98635d2ad6b94f6dfddf52ba09158a1a1c9f2
```

Same source-bound run:

```text
TypeScript                              PASS
Bot API mocked official contract        6 / 6 PASS
Telegram adapter/render                 7 / 7 PASS
clean real-Temporal Telegram regression PASS → Customer CREATED
Agent                                   false
MCP                                     false
```

Receipt: `Build/evidence/c2p-telegram-official-bot-api-deterministic-2026-09-04.md`.

This certifies the runner and Engine composition deterministically. It does **not** yet certify a real BotFather token/account or real `api.telegram.org` message delivery.

## Current physical gate

```text
BotFather /newbot
→ local TELEGRAM_BOT_TOKEN environment
→ local Docker Engines infrastructure
→ telegram-bot long-poll process
→ real private Telegram chat
→ RegisterNewCustomer
→ Customer CREATED
```

No `api_id`, `api_hash`, public URL, webhook, reverse proxy or paid hosting is required for this long-poll physical gate.

## Official-provider boundary

Telegram C2P uses only the official Bot API. WhatsApp remains official-route-only; browser automation, WhatsApp Web session emulation, QR-session scraping and reverse-engineered clients remain rejected.

Meta Cloud API versus Kapso remains a future WhatsApp provider implementation choice below `WhatsAppTransportPort`.

## Current Step-3 truth

Services S7 remains certified on `build/mk1-s7-appointment-services-integration`. S8 is still required for full G1 Services closure. Scheduler runtime remains un-certified.

## Non-claims

This snapshot does not yet claim:

- C2P Telegram official Bot API physical verification;
- production Telegram hosting;
- Telegram webhook mode;
- paid broadcast capacity;
- real Meta Cloud API / Kapso proof;
- public WhatsApp webhook reachability;
- Appointment over messaging;
- advisor handoff over messaging;
- outbound campaigns/promotions;
- Scheduler runtime certification;
- Agent/MCP availability;
- production readiness.
