global:
  scrape_interval: 1s
  evaluation_interval: 1s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: engines-platform-observer
    static_configs:
      - targets: ["platform-observer:8890"]
