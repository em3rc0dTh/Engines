# MK1 Evidence Registry on main

This folder is a navigation layer only. Source-bound receipts remain on the executable MK1 branches until promotion.

Primary current receipt locations:

```text
build/mk1-customer-channels-integrated/mk1/Build/evidence/
build/mk1-s7-appointment-services-integration/mk1/Build/evidence/
build/mk1-c4p-whatsapp-cloud-api-official/mk1/Build/evidence/
```

Never copy a receipt into `main` and then present the documentation commit as if that SHA were the executed runtime authority.
