# Build

Record executable implementation slices, source SHAs, dependency changes, packaging/runtime decisions and links to bounded evidence receipts. Code alone is never a certification.

Put immutable or source-bound receipts under `Build/evidence/`.
