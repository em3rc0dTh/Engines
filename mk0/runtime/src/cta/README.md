# CTA boundary

This directory contains replaceable channel/transport adapters for mk0.

The CTA layer may parse transport-specific input and convert it into canonical operation envelopes. It is not business authority and must not write application persistence directly.

B3 begins with the CLI channel.
