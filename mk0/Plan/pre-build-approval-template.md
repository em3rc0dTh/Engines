# mk0 — Pre-Build Approval Record Template

## Status

**TEMPLATE ONLY — NOT AN APPROVAL**

This document defines the minimum evidence required before `Build/README.md` may change from:

```text
NOT AUTHORIZED YET
```

to an explicitly approved Build state.

Copy this template into a dated approval record or complete an equivalent repository record when the project makes the authorization decision.

Do not treat the existence of this template as permission to scaffold or implement production code.

---

## 1. Approval identity

```text
Decision:            APPROVED | REJECTED | CHANGES_REQUIRED
Approval date:       YYYY-MM-DD
Approved commit SHA: <full repository commit SHA>
Approved by:         <project authority / review identity>
Record authored by:  <identity>
```

Required rule:

> `Decision` must be explicitly `APPROVED` before Build begins.

Blank fields, inferred approval, chat-only approval without repository record, or “looks ready” are not Build authorization.

---

## 2. Approved mk0 scope

Confirm the approved first proof remains:

```text
Postman / CLI
→ CTA Adapter
→ real Temporal Service
→ RegisterNewCustomer Workflow
→ Activities / ports
→ PostgreSQL + MongoDB + optional AttachmentStore
→ canonical state/result
```

Confirm:

- [ ] `RegisterNewCustomer` is the first laboratory Workflow, not the complete Engines product.
- [ ] No Agent/Hermes is part of the mk0 execution authority.
- [ ] No Scheduler/Appointment/ResourceReservation behavior is introduced by this Workflow.
- [ ] No application/web framework becomes orchestration authority.
- [ ] CTA implementations remain replaceable adapters.

---

## 3. Design package reviewed

Record the exact reviewed paths/versions:

```text
Design/01-system-architecture.md
Design/02-register-new-customer-contract.md
Design/03-temporal-workflow.md
Design/04-persistence-boundaries.md
Design/05-mk0-persistence-profile.md
Design/06-mk0-closure-decisions.md
```

Checklist:

- [ ] Interactive partial/intent-only start semantics accepted.
- [ ] CTA structural/session validation boundary accepted.
- [ ] Versioned RegistrationPolicy semantics accepted.
- [ ] Business-scoped session idempotency accepted.
- [ ] Initial-start fingerprint vs later Update semantics accepted.
- [ ] Hard/soft/non-unique duplicate semantics accepted.
- [ ] Both `CREATED` and `ALREADY_EXISTS` pass required audit/finalization before success.
- [ ] Temporal deterministic replay/version-compatibility invariant accepted.
- [ ] PostgreSQL/MongoDB/AttachmentStore/Temporal authority boundaries accepted.

Design review notes:

```text
<fill before approval>
```

---

## 4. Test contract reviewed

Reviewed path:

```text
Test/README.md
```

Confirm the future runtime suite must prove:

- [ ] real Temporal Service;
- [ ] real Task Queue + Worker;
- [ ] real Workflow + Activities;
- [ ] Query + Update behavior;
- [ ] exact start replay;
- [ ] same-session conflicting initial start rejection;
- [ ] cross-business opaque-key isolation;
- [ ] multi-round data collection;
- [ ] hard duplicate and soft duplicate behavior;
- [ ] PostgreSQL retry/recovery;
- [ ] mandatory MongoDB audit-before-success behavior;
- [ ] exhausted MongoDB required-audit failure cannot become success;
- [ ] AttachmentStore retry/integrity when attachment cases are enabled;
- [ ] CTA disconnect/reconnect continuity;
- [ ] Worker restart continuity;
- [ ] zero scheduling side effects.

Test review notes:

```text
<fill before approval>
```

---

## 5. Golden Dataset reviewed

Approved dataset version:

```text
mk0.golden.register-customer.v0
```

Reviewed files:

```text
golden-dataset/README.md
golden-dataset/register-new-customer-v0.json
```

Confirm:

- [ ] dataset is machine-readable;
- [ ] RegistrationPolicy fixture is explicit;
- [ ] business-scoped idempotency profile is explicit;
- [ ] initial-start fingerprint profile is explicit;
- [ ] mandatory audit-success profile is explicit;
- [ ] GD-001 through GD-018 are reviewed;
- [ ] unresolved attachment fixture hashes are explicitly marked and their disposition is recorded below;
- [ ] expected behavior was not generated by reverse-engineering implementation output.

Attachment fixture disposition:

```text
Choose one and explain:

A. attachment cases enabled in mk0 Build
   → AttachmentStore technology will be selected before B7
   → fixture binaries + SHA-256 values will be frozen before attachment runtime certification

B. attachment implementation deferred within mk0 Build sequence
   → B0–B6 may proceed after authorization
   → B7/attachment runtime cases remain gated until technology + fixture hashes are frozen

Selected disposition: <fill before approval>
Rationale:            <fill before approval>
```

---

## 6. Explicit BUILD_TIME decisions accepted as deferred

These are not architecture gaps if the invariant/selection criteria are already frozen:

```text
Temporal SDK language/version
SDK-specific workflow versioning mechanism
namespace/topology
Task Queue naming/topology
Activity retry numeric values
Activity timeout/heartbeat numeric values
payload converter/codec
PostgreSQL driver/ORM and DDL
MongoDB driver/ODM/index implementation
AttachmentStore physical technology when enabled
attachment size/count/TTL limits
local/deployment container topology
credentials/secrets mechanism
```

For each item that remains deferred, confirm:

- [ ] its architectural authority is already known;
- [ ] Build cannot use it to redefine Design/Test/Golden behavior;
- [ ] its selection will be recorded before the first Build step that depends on it.

Additional deferred choices:

```text
<fill before approval>
```

---

## 7. Pre-Build blockers

Before `Decision = APPROVED`, this list must be empty or every item must be explicitly reclassified as a valid BUILD_TIME decision with rationale.

```text
BLOCKER-01: <fill or NONE>
BLOCKER-02: <fill or NONE>
BLOCKER-03: <fill or NONE>
```

Reviewer conclusion:

```text
<fill before approval>
```

---

## 8. First authorized Build boundary

If approved, the first authorized sequence remains:

```text
B0  runtime/repository skeleton with no web framework
B1  versioned RegistrationPolicy + canonical interaction contracts
B2  full Temporal local/runtime topology + Web UI
B3  first CTA Adapter / CLI
B4  Temporal Worker + Task Queue + interactive RegisterNewCustomer Workflow
B5  PostgreSQL policy/duplicate lookup + Customer persistence Activities
B6  MongoDB interaction/audit Activities
B7  AttachmentStore implementation if enabled
B8  complete orchestration
B9  Postman-compatible CTA Adapter
B10 end-to-end laboratory verification
B11 failure/retry/restart/reconnect certification
B12 mk0 stable candidate
```

First authorized ticket:

```text
<normally B0; fill explicitly>
```

Any scope beyond the approved ticket sequence requires another decision rather than silently entering the Build.

---

## 9. Authorization statement

Only use this statement after every required field above has been reviewed:

```text
MK0 BUILD AUTHORIZED

The project approves the mk0 Design/Test/Golden contracts at commit <SHA>.
Build may begin with <first authorized ticket>.
Build-time technology choices remain subordinate to the approved behavioral and authority contracts.
No additional Workflow/Engine scope is authorized by this decision.
```

If the decision is not approved, record instead:

```text
MK0 BUILD NOT AUTHORIZED
Reason: <explicit blocker or required change>
```
