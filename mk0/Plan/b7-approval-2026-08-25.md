# B7 — Project authority approval

## Status

**APPROVED — 2026-08-25**

This record authorizes B7 after the attachment-free mk0 core achieved integrated Golden certification.

## Approved baseline

```text
Repository: em3rc0dTh/Engines
Approved baseline branch: cert/mk0-core-golden-release
Approved baseline SHA: 5e36064936a4621191412d8676b957bc0998ad5e
Integrated certified runtime source SHA: f0896c58d918e8f3971c78867870243e16a8b604
Integrated Golden run: 32873848134
Verdict: ATTACHMENT_FREE_CORE_GOLDEN_PASS
```

## Project-authority decision

The project authority explicitly approved continuing B7 using the following local laboratory environment:

```text
Host: Windows + WSL2
Runtime environment: Linux inside WSL2
Primary local deployment: Docker Compose
Manual CTA client: Postman on Windows
Network scope: localhost/local WSL only
Public tunnels/exposure: OUT OF SCOPE for this gate
Temporal: local/containerized
PostgreSQL: local/containerized
MongoDB: local/containerized
AttachmentStore: filesystem-backed content-addressed local store
Cloud/object-storage dependency: NONE for mk0 certification
```

The intended user test experience after B7 certification is:

```text
git clone / git pull
→ cd mk0/runtime
→ docker compose up --build -d
→ docker compose ps
→ Postman → localhost CTA
→ Temporal Workflow → PostgreSQL + MongoDB + AttachmentStore
```

Exact user-facing commands are not declared certified until the Compose stack itself passes its runtime gate.

## Physical AttachmentStore profile

Approved:

```text
AttachmentStorePort remains provider-neutral
physical mk0 adapter = filesystem-backed content-addressed storage
logical ingress identity = opaque ingressRef
logical committed identity = opaque attachmentId
binary integrity identity = lowercase SHA-256
atomic publication = same-filesystem temporary write + rename
business attachment references = PostgreSQL
mandatory ATTACHMENT_COMMITTED evidence = MongoDB
Workflow orchestration = Temporal
```

## Laboratory limits

Approved for mk0 only:

```text
maximum attachment count per registration = 4
maximum single attachment byte length = 1 MiB
maximum total attachment bytes per registration = 2 MiB
staged ingress TTL = 15 minutes
integrity algorithm = SHA-256
Golden fixture media types = image/png, text/plain
```

These are laboratory certification limits, not production policy.

## Fixture plan

Approved:

```text
F1 synthetic-id-front-v1.png
F2 synthetic-supporting-document-v1.txt
```

The exact bytes, byte lengths and SHA-256 values must be frozen in B7.1 before attachment runtime certification.

## Authorized scope

B7 may implement:

```text
filesystem AttachmentStore port/adapter
stage / resolve / retrieve / commit / expiry / reconciliation
synthetic fixture binaries and frozen hashes
PostgreSQL customer attachment references
Temporal attachment Activities
RegisterNewCustomer attachment path
MongoDB ATTACHMENT_COMMITTED success gate
GD-003 / GD-004 / GD-009 / GD-010
full 18-case Golden rerun
local WSL Docker Compose laboratory
```

Still out of scope:

```text
public tunnels/ngrok
production cloud storage selection
production deployment
OCR/extraction
malware scanning platform
Agent/Hermes
Scheduler
new business Workflows
```

## Build sequence authorized

```text
B7.0  authority + physical profile closure                  APPROVED BY THIS RECORD
B7.1  fixture bytes + SHA-256/length freeze                 AUTHORIZED
B7.2  AttachmentStore port + filesystem adapter             AUTHORIZED
B7.3  stage/resolve/retrieve + TTL/reconciliation            AUTHORIZED
B7.4  retry-safe commit identity                            AUTHORIZED
B7.5  PostgreSQL customer_attachment_refs                   AUTHORIZED
B7.6  Temporal Activities + Workflow attachment path        AUTHORIZED
B7.7  Mongo ATTACHMENT_COMMITTED success gate               AUTHORIZED
B7.8  GD-003 / GD-004                                       AUTHORIZED
B7.9  GD-009                                                AUTHORIZED
B7.10 GD-010                                                AUTHORIZED
B7.11 full 18-case Golden rerun                             AUTHORIZED
B7.12 full mk0 certification receipt                        AUTHORIZED
```

## Explicit authorization

```text
Decision: APPROVED
Decision date: 2026-08-25
Physical AttachmentStore profile: filesystem-backed content-addressed local mk0 store
Local test environment: WSL2 + Docker Compose + Windows Postman
Public exposure: not part of this gate
Explicit authorization to implement B7: YES
```

> **B7 is authorized. Full mk0 is not certified until all 18 Golden cases pass in the approved local-compatible runtime profile.**
